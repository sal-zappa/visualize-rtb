{
  "name": "visualize-rtb",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf build/* && tsc",
    "dist": "rm -rf build/* && tsc --project tsconfig.dist.json && mkdir -p dist && cp -r build/* src/manifest.json src/popup/* dist",
    "test": "tsc && jasmine --config=jasmine.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sal-zappa/visualize-rtb.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/sal-zappa/visualize-rtb/issues"
  },
  "homepage": "https://github.com/sal-zappa/visualize-rtb#readme",
  "dependencies": {
    "jasmine": "^3.6.3",
    "typescript": "^4.1.3",
    "@types/jasmine": "^3.6.2",
    "@types/chrome": "0.0.127"
  }
}
